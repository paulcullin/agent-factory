# Root of the template repo. The node-ts starter has its own .gitignore.
.DS_Store
*.log
node_modules/
.claude/settings.local.json

# Isolated per-issue/per-agent git worktrees created by /implement and /sprint
# (see docs/WORKFLOW.md). Transient working state, never committed.
.claude/worktrees/
